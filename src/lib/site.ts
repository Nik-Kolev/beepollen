// The Vercel demo, not a production host — metadata and JSON-LD need an
// absolute origin at build time and this is the only one that exists yet.
export const SITE_URL = "https://beepollen-wheat.vercel.app";
